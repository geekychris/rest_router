# NOTE: dev placeholders only — replace with sealed-secrets / external-secrets
# / Vault for real deployments.
apiVersion: v1
kind: Secret
metadata:
  name: rest-router-secrets
  namespace: gateway
type: Opaque
stringData:
  ADMIN_API_KEY: "replace-me-in-prod"
